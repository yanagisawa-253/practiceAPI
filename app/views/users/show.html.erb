<div class="container">
  <div class="row">
    <div class="col-md-8 offset-md-1 mt-5">
      <h2>あなたが投稿したもの</h2>
        <table class="table mt-3">
          <thead>
            <tr>
              <th>Title</th>
              <th>opinion</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <% @book.each do |book| %>
              <td><%= book.title %></td>
              <td><%= book.body %></td>
            </tr>
          </tbody>
          <% end %>
        </table>
    </div>
  </div>
</div>